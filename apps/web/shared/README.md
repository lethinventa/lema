# shared

Componentes, composables e utilitários usados por mais de uma feature — sem regra de negócio de domínio, sem conhecimento de SDK de vendor (isso é `lib/`).

```
shared/
├── components/
├── composables/
└── utils/
```

Ver `docs/architecture/decisions/ADR-002-feature-folder-structure.md`.
