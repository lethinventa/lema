# config

Configuração geral da aplicação: validação de variáveis de ambiente, constantes não-secretas. Nunca código de feature, nunca SDK de vendor (isso é `lib/`).

Ver `docs/architecture/decisions/ADR-002-feature-folder-structure.md`.
